import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  pickerStyle: {
    width: '100%',
    height: '100%',
    borderRadius: 3,
  },
  pickerContainerView: {
    height: 42,
    marginBottom: 10,
    marginTop:0,
    borderRadius: 3,
    paddingHorizontal: 1,
    backgroundColor: '#f5f5f5',
  },
});
