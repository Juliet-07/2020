import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width:'100%',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    // backgroundColor: 'red',
  },
  initials: {
    backgroundColor: '#0753AB',
    borderRadius: 100,
    width: 80,
    height: 80,
    justifyContent: 'center',
    alignItems: 'center',
  },
  initialsText: {fontWeight: 'bold', fontSize: 30, color: 'white'},
});
